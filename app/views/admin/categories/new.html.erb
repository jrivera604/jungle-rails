<h1>New Category</h1>

<%= form_with(model: [:admin, @category], url: admin_categories_path) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit %>
<% end %>

<%= link_to 'Back', admin_categories_path %>
